import type { SettingsState } from '../SettingsState/SettingsState.ts'

export const handleResizerPointerUp = (state: SettingsState, eventX: number, eventY: number): SettingsState => {
  return state
}
